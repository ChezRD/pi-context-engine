{
  "name": "pi-deepseek-cache",
  "version": "0.1.0",
  "description": "DeepSeek prompt-cache telemetry and context coordination for Pi coding agent",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi-extension",
    "deepseek",
    "cache",
    "context-prune"
  ],
  "license": "MIT",
  "files": [
    "src/**/*.ts",
    "README.md",
    "LICENSE"
  ],
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.75.5",
    "typebox": "1.1.38",
    "typescript": "5.9.3"
  }
}
